# Shared GitHub workflows

This repository contains reusable GitHub Actions workflows and repository defaults for `aliawilkinson` projects.

Changes to reusable workflows are reviewed through pull requests and published through stable major-version tags such as `v1`.
